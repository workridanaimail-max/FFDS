# Pinned so the saved model artifacts unpickle without version-drift warnings.
# Regenerate the .pkl files (python backend/train_model.py) if you bump scikit-learn.
Flask==3.1.3
scikit-learn==1.9.0
pandas==2.2.3
numpy==2.4.6
imbalanced-learn==0.14.2
joblib==1.4.2

# Gradient boosting candidates. Required at serve time too -- unpickling the
# saved model needs whichever library produced it.
lightgbm==4.6.0
xgboost==3.3.0
catboost==1.2.10

# Testing
pytest==9.0.2
